import { AudioMetadataDirective } from './audio-metadata.directive';

describe('AudioMetadataDirective', () => {
  it('should create an instance', () => {
    const directive = new AudioMetadataDirective();
    expect(directive).toBeTruthy();
  });
});
